# Core dependencies
celery[redis]==5.3.4
redis==5.0.1

# NumPy - Pin to version 1.x for PyTorch compatibility
numpy<2.0.0,>=1.24.0

# Audio processing - Whisper for transcription
openai-whisper==20231117
ffmpeg-python==0.2.0

# PyTorch for Apple Silicon (M1/M2/M3)
# Install specific version for MPS (Metal Performance Shaders) support
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.1.2
torchaudio==2.1.2

# Additional audio processing libraries
librosa==0.10.1
soundfile==0.12.1

# Utilities
python-dotenv==1.0.0
pydantic==2.5.2
pydantic-settings==2.1.0

# Monitoring (optional)
flower==2.0.1

# macOS specific optimizations (optional - comment out if causing issues)
# pyobjc-core==10.0  # For better macOS integration
# pyobjc-framework-Metal==10.0  # Direct Metal framework access
# pyobjc-framework-MetalPerformanceShaders==10.0  # MPS framework